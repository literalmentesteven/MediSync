<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MediSync"
             x:Class="MediSync.App">
    <Application.Resources>
        <ResourceDictionary>
            
            <!-- PALETA DE COLORES CLINICAL CLEAN -->
            <Color x:Key="MediCyan">#00B2CA</Color>
            <Color x:Key="MediDarkGray">#333333</Color>
            <Color x:Key="MediMediumGray">#888888</Color>
            <Color x:Key="MediLightGray">#F5F7FA</Color>
            <Color x:Key="MediWhite">#FFFFFF</Color>
            <Color x:Key="MediRed">#E57373</Color>
            <Color x:Key="MediGreen">#81C784</Color>

            <!-- ESTILOS GLOBALES -->
            
            <!-- Labels -->
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource MediDarkGray}" />
            </Style>
            
            <!-- Botones con bordes redondeados -->
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource MediCyan}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="20" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Padding" Value="20,10" />
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal" />
                            <VisualState x:Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Property="Opacity" Value="0.8" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <!-- Inputs dentro de Bordes -->
            <Style TargetType="Entry">
                <Setter Property="TextColor" Value="{StaticResource MediDarkGray}" />
                <Setter Property="PlaceholderColor" Value="{StaticResource MediMediumGray}" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>